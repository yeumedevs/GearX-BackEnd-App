<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gearx.feature.security.mapper.RevokedTokenMapper">

    <select id="isRevoked" parameterType="string" resultType="int">
        SELECT COUNT(1)
        FROM revoked_token
        WHERE token = #{token}
    </select>

    <delete id="deleteExpired">
        <![CDATA[
        DELETE FROM revoked_token WHERE expires_at < CURRENT_TIMESTAMP
        ]]>
    </delete>

    <insert id="insert">
        INSERT INTO revoked_token (
        token,
        expires_at
        ) VALUES (
        #{token},
        #{expiresAt}
        )
    </insert>

</mapper>

